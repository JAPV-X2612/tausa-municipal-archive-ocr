services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist the ChromaDB vector store across container restarts.
      - chroma_data:/app/chroma_db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  chroma_data:
